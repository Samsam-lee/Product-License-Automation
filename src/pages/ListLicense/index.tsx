import FrameContainer from '../../components/Frame/FrameContainer';
import ListLicenseContainer from '../../components/ListLicense/ListLicenseContainer';

const ListLicense = () => (
  <FrameContainer>
    <ListLicenseContainer />
  </FrameContainer>
);

export default ListLicense;

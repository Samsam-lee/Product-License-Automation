import FrameContainer from '../../components/Frame/FrameContainer';
import RequestLicenseContainer from '../../components/RequestLicense/RequestLicenseContainer';

const RequestLicense = () => (
  <FrameContainer>
    <RequestLicenseContainer />
  </FrameContainer>
);

export default RequestLicense;
